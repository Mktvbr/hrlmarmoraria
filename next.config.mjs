/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
        qualities: [25, 50, 75, 100],
    },
    domains: ['localhost', 'hrl.com.br', 'www.hrl.com.br'],
};


export default nextConfig;
